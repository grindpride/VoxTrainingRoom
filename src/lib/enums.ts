export enum MonthTypes {
  Prev = 'prev',
  Current = 'current',
  Next = 'next'
}

export enum ButtonTypes {
  Submit = 'submit',
  Default = 'default',
  Delete = 'delete'
}

export enum InputType {
  input = 'input',
  textarea = 'textarea'
}
